# Faster-Whisper worker

`portuwana-faster-whisper.exe` and its `_internal` runtime are generated by
`tools/faster-whisper-worker/build-worker.ps1`. Generated runtime files are
kept local and ignored by Git; this marker documents the reproducible build.
