# Faster-Whisper small model

The multilingual `Systran/faster-whisper-small` snapshot is copied here by
`tools/faster-whisper-worker/build-worker.ps1`. Generated model files are kept
local and ignored by Git; this marker keeps Tauri's development resource glob
valid before the model is provisioned.
